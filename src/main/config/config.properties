# Packaged reference defaults. The console persists its working-directory
# config.properties file, while the visual session uses these same defaults in code.
numPlayers=2
playerNames=Alice,Bob
scoreLimit=5000
openingScoreLimit=1000
